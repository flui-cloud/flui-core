apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ISSUER_NAME}}
  labels:
    managed-by: flui-cloud
spec:
  acme:
    server: {{ACME_SERVER}}
    email: {{ACME_EMAIL}}
    privateKeySecretRef:
      name: {{PRIVATE_KEY_SECRET_REF}}
    solvers:
{{DNS01_SOLVERS}}
      - http01:
          ingress:
            class: traefik
            # See cluster-issuer.yaml: a NodePort solver Service is both an open
            # node port and a quota violation in a restricted namespace.
            serviceType: ClusterIP
            ingressTemplate:
              metadata:
                annotations:
                  traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
                  traefik.ingress.kubernetes.io/router.priority: "20000000"
